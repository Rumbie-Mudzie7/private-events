<!DOCTYPE html>
<html>
  <head>
    <%# meta tag for responsiveness. %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%# Dynamic page title %>
    <title><%= content_for?(:title) ? yeild(:title) : "PrivateEvents"%></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "PrivateEvents"%>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header>
      <%= render 'layouts/nav'%>
    </header>

    <% flash.each do |name, message| %>
      <div class="<%= name %> text-center bg-light"><%= message %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
